<%= form_with(model: funcionario, local: true) do |form| %>

  <% if funcionario.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(funcionario.errors.count, "error") %> prohibited this funcionario from being saved:</h2>

      <ul>
        <% funcionario.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <%= form.label :ambiente_id, 'Departamento' %>
  <%= form.collection_select :ambiente_id, @departamentos, :id, :nome, prompt: "Selecione um departamento" %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
