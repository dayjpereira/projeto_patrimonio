<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Projeto Patrimônio" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  

    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>

  </head>

  <body class="bg-gray-100 min-h-screen flex">



    <!-- Mensagem flash -->
    <% if notice %>
      <div class="fixed top-4 right-4 bg-green-500 text-white px-4 py-2 rounded shadow z-50">
        <%= notice %>
      </div>
    <% end %>

    <!-- Sidebar Lateral -->
    <aside class="w-60 bg-gray-900 text-white flex flex-col p-5 space-y-4">
      <h1 class="text-xl font-bold mb-6">Senge-RJ</h1>

      <%= link_to '🏠 Patrimônios', patrimonios_path, class: "hover:text-gray-300" %>
      <%= link_to '🏢 Localização', ambientes_path, class: "hover:text-gray-300" %>
      <%= link_to '👥 Funcionários', funcionarios_path, class: "hover:text-gray-300" %>
      <%= link_to '⚙️ Configurações', "#", class: "hover:text-gray-300" %>
      <%= link_to '🚪 Sair', "#", class: "hover:text-red-400" %>
    </aside>

    <!-- Conteúdo Principal -->
    <main class="flex-1 p-8">
      <%= yield %>
    </main>

  </body>
</html>
